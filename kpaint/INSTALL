If installing into kdeapps tree:

move your old kpaint src out of the way and extract this in it's place.
Rerun configure and type make then make install.

If install standalone:

make
make install

If it doesn't work or you want to remove kpaint then type:
make uninstall

