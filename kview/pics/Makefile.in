FILES =  diashow.xpm random.xpm resize.xpm

PICSDIR = @prefix@/lib/pics/toolbar
INSTALL_DIR = @INSTALL@ -d -m 0755
INSTALL_DATA = @INSTALL@ -m 644

all:

install:

uninstall:
	for i in $(FILES); do \
	 rm -f $(PICSDIR)/$$i ; \
	done
